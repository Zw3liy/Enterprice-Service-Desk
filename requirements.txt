# Enterprise Service Desk â€” runtime dependencies.
#
# Verified by scanning every first-party import under apps/service_desk/
# and ticketing/ (see docs/engineering/ROADMAP.md, CI-01): the tested
# codebase uses only the Python standard library plus Django itself.
# Pinned to the exact version this project has been developed and
# tested against.

Django==5.2.16
dj-database-url==2.3.0
psycopg[binary]==3.2.13
gunicorn==23.0.0
whitenoise==6.9.0
